# Streamlit Community Cloud — sirf yehi teen lines. Kuch aur pin na karein:
# pandas/pillow pin karne se Python 3.14 par pip source-compile mein atak jaata hai,
# aur code mein wo direct import bhi nahi hote (streamlit khud le aata hai).
streamlit==1.62.0
supabase>=2.10,<3

# Google / Gmail sign-in (st.login) ke liye laazmi — iske baghair
# StreamlitMissingAuthlibError aata hai. cryptography khud isi ke sath aa jaata hai.
Authlib>=1.3.2,<2
